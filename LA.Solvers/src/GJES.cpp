#include "GJES.hpp"

namespace LA {
	namespace Solvers {
		template <typename T>
		GJES<T>::GJES() {}

		/*
		* ---------------------------------------------------------------------------------------- 
		* solveWithPartialPivoting
		*  Summary: Perform GJE with Partial Pivoting.
		* ---------------------------------------------------------------------------------------- 
		*/
		template <typename T>
		Core::Matrix<T> GJES<T>::solveWithPartialPivoting(const Core::Matrix<T>& A, const Core::Matrix<T>& b) {
			Core::Matrix<T> result;
			
			return result;
		}
	}
}
